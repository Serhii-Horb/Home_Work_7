public class Ex2 {    /* У вас на банковском счету N долларов.       Вы хотите снять все, но банк разрешает снять только сумму, которая является делителем текущей       суммы на счету и меньше текущей суммы.       Если вы снимаете максимально возможную сумму каждый день, сколько дней вам понадобится, чтобы забрать       все свои деньги из банка?       Примечание: если оставшаяся сумма равна 1, вы можете снять ее. */    public static void main(String[] args) {        int balance = 21;        int days = calculateDays(balance);        System.out.println("Для снятия всех денег потребуется " + days + " дней.");    }    private static int calculateDays(int balance) {        int days = 0;        while (balance > 1) {            int maxDivisor = findMaxDivisor(balance);            if (maxDivisor > 0) {                balance -= maxDivisor;                days++;            } else {                break;            }        }        if (balance == 1) {            days++;        }        return days;    }    private static int findMaxDivisor(int number) {        for (int i = number - 1; i > 0; i--) {            if (number % i == 0) {                return i;            }        }        return 0;    }}